export enum VIEW {
    LIST = 'list',
    GRID = 'grid',
}

export enum INVENTORY_TYPE {
    INVENTORY = 'inventory',
    DIRECT = 'direct',
    CONSIGNMENT = 'consignment',
}


export enum PrDocType {
    BLANK = 0,
    TEMPLATE = 1,
}


export enum TaxType {
    NONE = 'none',
    INCLUDED = 'included',
    ADD = 'add',
}

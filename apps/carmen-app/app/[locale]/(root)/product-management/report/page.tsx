import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Report",
};

export default function ReportPage() {
    return (
        <div>
            <h1>Report</h1>
        </div>
    )
}
